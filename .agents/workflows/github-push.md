---
description: Push latest changes to GitHub after every code edit
---
// turbo-all

1. Stage all changes in the Financial Agent project:
```
git add .
```

2. Commit with a descriptive message about the latest change (use the actual change as the message):
```
git commit -m "feat: <description of change>"
```

3. Push to origin main:
```
git push
```

All three commands run from `C:\Chrome Download\AntiGravity\Financial Agent`.
